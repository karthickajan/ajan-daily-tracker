╔═══════════════════════════════════════════════════════════════════════════════╗
║           AJAN DAILY TRACKER - COMPLETE SUPABASE SETUP GUIDE                  ║
╚═══════════════════════════════════════════════════════════════════════════════╝

YOUR SUPABASE PROJECT DETAILS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Project Name: Daily Tracker
  Project URL: https://suxszrvczmdfajekqyrw.supabase.co
  Anon Key: sb_publishable_PAqRM1mxu_St436Be0NwVw_Aj7MW-kp

  ⚠️  DO NOT SHARE THIS KEY PUBLICLY
  ⚠️  It's only for anonymous read/write to your tracker table


STEP 1️⃣  CREATE THE TABLE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Go to: https://app.supabase.com
2. Select "Daily Tracker" project
3. Click: SQL Editor (left sidebar)
4. Click: New Query
5. Copy & Paste this SQL:

┌─────────────────────────────────────────────────────────────────────────────┐
│ CREATE TABLE public.tracker_entries (                                        │
│   id BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,                   │
│   device_id TEXT NOT NULL,                                                  │
│   date TEXT NOT NULL,                                                       │
│   sleep_6h BOOLEAN DEFAULT false,                                           │
│   sleep_hours FLOAT DEFAULT 0,                                              │
│   bathed BOOLEAN DEFAULT false,                                             │
│   hair_controlled BOOLEAN DEFAULT false,                                    │
│   ate_enough BOOLEAN DEFAULT false,                                         │
│   protein_ok BOOLEAN DEFAULT false,                                         │
│   dsa_studied BOOLEAN DEFAULT false,                                        │
│   dsa_hours FLOAT DEFAULT 0,                                                │
│   sysdesign_studied BOOLEAN DEFAULT false,                                  │
│   sysdesign_hours FLOAT DEFAULT 0,                                          │
│   deepwork_90min BOOLEAN DEFAULT false,                                     │
│   solved_designed BOOLEAN DEFAULT false,                                    │
│   low_distraction BOOLEAN DEFAULT false,                                    │
│   progress FLOAT DEFAULT 0,                                                 │
│   created_at TIMESTAMP DEFAULT NOW(),                                       │
│   updated_at TIMESTAMP DEFAULT NOW()                                        │
│ );                                                                           │
│                                                                             │
│ -- Create index for faster queries                                          │
│ CREATE INDEX idx_device_date ON public.tracker_entries(device_id, date);   │
└─────────────────────────────────────────────────────────────────────────────┘

6. Click: Run (or Ctrl+Enter)
7. ✅ You should see "Success" message


STEP 2️⃣  ENABLE ROW LEVEL SECURITY (RLS)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Go to: Authentication → Policies (left sidebar)
2. Or click: Table Editor → tracker_entries → Policies tab
3. Click: Enable RLS
4. Create policies (see STEP 3 below)


STEP 3️⃣  CREATE POLICIES (PERMISSIONS)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 Your tracker doesn't use login - it uses "device_id" to identify you.
   So we create simple policies that allow all operations.

1. Go to: SQL Editor → New Query
2. Copy & Paste this SQL:

┌─────────────────────────────────────────────────────────────────────────────┐
│ -- Enable Row Level Security                                               │
│ ALTER TABLE public.tracker_entries ENABLE ROW LEVEL SECURITY;              │
│                                                                             │
│ -- Policy 1: Allow INSERT                                                  │
│ CREATE POLICY "allow_insert"                                               │
│ ON public.tracker_entries FOR INSERT                                       │
│ WITH CHECK (true);                                                          │
│                                                                             │
│ -- Policy 2: Allow UPDATE                                                  │
│ CREATE POLICY "allow_update"                                               │
│ ON public.tracker_entries FOR UPDATE                                       │
│ USING (true) WITH CHECK (true);                                            │
│                                                                             │
│ -- Policy 3: Allow SELECT                                                  │
│ CREATE POLICY "allow_select"                                               │
│ ON public.tracker_entries FOR SELECT                                       │
│ USING (true);                                                               │
│                                                                             │
│ -- Policy 4: Allow DELETE                                                  │
│ CREATE POLICY "allow_delete"                                               │
│ ON public.tracker_entries FOR DELETE                                       │
│ USING (true);                                                               │
└─────────────────────────────────────────────────────────────────────────────┘

3. Click: Run
4. ✅ You should see "Success" message


STEP 4️⃣  START YOUR TRACKER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Open terminal and run:

  cd "/Users/karthicks7/Desktop/Ajan Daily Tracker"
  python daily_tracker_web.py

Then open: http://localhost:5000

Your tracker will now:
  ✅ Save entries to Supabase (cloud)
  ✅ Load entries from Supabase (sync)
  ✅ Work offline + sync when online
  ✅ Share data across all your devices


WHAT EACH POLICY DOES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 POLICY 1: "allow_insert"
   └─ FOR INSERT WITH CHECK (true)
   └─ Anyone can INSERT new entries
   └─ "true" = Always allow (no conditions)

✏️  POLICY 2: "allow_update"
   └─ FOR UPDATE USING (true) WITH CHECK (true)
   └─ Anyone can UPDATE existing entries
   └─ USING (true) = Allow reading existing rows
   └─ WITH CHECK (true) = Allow updating to new values

👁️  POLICY 3: "allow_select"
   └─ FOR SELECT USING (true)
   └─ Anyone can SELECT (read) entries
   └─ "true" = Show all rows

🗑️  POLICY 4: "allow_delete"
   └─ FOR DELETE USING (true)
   └─ Anyone can DELETE entries
   └─ "true" = Allow deleting any row


IMPORTANT CONCEPTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔐 Row Level Security (RLS)
   └─ Controls who can see/edit which rows
   └─ Without policies = No access
   └─ With policies = Access based on conditions

📱 device_id
   └─ Unique identifier for your device/browser
   └─ Prevents conflicts between multiple devices
   └─ Stored with each entry in Supabase

🌐 Anonymous Access
   └─ No login required
   └─ Uses "Anon Key" from your Supabase project
   └─ Perfect for personal tracker

💾 Offline Support
   └─ App caches data locally (localStorage)
   └─ When online: syncs with Supabase
   └─ When offline: works with local cache


VERIFY IT'S WORKING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Open tracker at http://localhost:5000
2. Toggle some checkboxes
3. Click "Save Today's Progress"
4. Go to Supabase: Table Editor → tracker_entries
5. You should see your data in the table! ✅

If you see data in Supabase = Everything works! 🎉


TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ "Table does not exist"
   └─ Run Step 1 (CREATE TABLE)

❌ "Permission denied"
   └─ You didn't create policies
   └─ Run Step 3 (CREATE POLICIES)

❌ "CORS error" in browser console
   └─ Go to Settings → CORS configuration
   └─ Add: http://localhost:5000

❌ Data not saving
   └─ Check browser console (F12 → Console tab)
   └─ Look for error messages
   └─ Verify your Anon Key is correct in the code


NEXT STEPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Complete steps 1-4 above
✅ Start tracking your habits!
✅ Your data syncs to the cloud automatically
✅ Access from any device
✅ Never lose your data


QUESTIONS?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 Supabase Docs: https://supabase.com/docs
🔒 RLS Guide: https://supabase.com/docs/guides/auth/row-level-security
📧 Check the comments in daily_tracker_web.py for more details

